// DEFINE YOUR FUNCTION BELOW:
function isShortsWeather(temp) {
    // Short answer
    // return temp >= 75;
    
    // Long answer.
    if (temp >= 75) {
        return true;
    } else {
        return false;
    }
}
